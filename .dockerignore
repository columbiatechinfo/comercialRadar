# Perfis descartaveis dos navegadores com tela: rodam como --user 0 e viram
# arquivos root que o `docker build` nem consegue ler no contexto. Foi o que
# quebrou o build da API em 03/09/2026 com "permission denied".
.browser_profiles/

# Ruido que nunca deve ser embutido na imagem.
.git/
__pycache__/
*.pyc
node_modules/
.env
.env.*
*.log
