# Retomar — 14/09/2026 · o que está em produção

> Marca git: `producao-2026-09-14`. Desenho do fluxo para leigos: `docs/fluxo-canoas-14-09-2026.html`.
> Publicação: `docs/PRODUCAO-SEEK.md`.

## No ar

| O quê | Onde |
|---|---|
| SEEK (home) e extração | `https://a2lsolucoes.com/seek/` · `/seek/extrair` — nginx da VPS + túnel `seek-tunel` do i9 |
| Mesmo sistema, endereço interno | `https://a2l-server-main-i914hx-pc-1.tail7e301b.ts.net:8445/` (tailnet) |
| API | contêiner `radar-comercial-api`, i9 `127.0.0.1:7740` → 7720, imagem construída deste repositório |
| IA | vLLM `ia-principal` na Spark `192.168.3.20:7400`, contexto 32.768 tokens |

## Canoas: estado dos vereditos (14/09/2026, 14h40)

| | Ligações |
|---|---|
| Julgadas no prompt novo | 23.960 |
| Aprovadas | 3.252 (todas "SIM") |
| Revisão humana | 7.930 |
| Reprovadas | 12.778 |

Toda ligação de Canoas qualificada SIM ou SIM com análise humana que tem registro vinculado e busca web está julgada. Ficam de fora só as que não têm nenhum registro vinculado.

## Decisões do dono do produto (13 e 14/09/2026)

1. **Busca web pelo Camoufox com sessão quente** — DuckDuckGo e Yahoo; Google só de reserva, também pelo Camoufox, a 20 buscas/min, com pausa de 1 h antes de cada rodada (o Google bloqueia os IPs depois de ~45 min).
2. **Prompt novo da avaliação** — a IA diz em campo próprio se as fotos mostram o comércio (`fotos.confirmam`) e quais resultados da busca confirmam um registro (`busca[].confirma`); motivo sem limite de frases.
3. **Avaliações do prompt antigo apagadas** (9.426, cópia em `radar_comercial.ligacao_veredito_antes_prompt_novo`) e tudo rejulgado; a coleta (busca, fotos, vínculos) ficou.
4. **SEEK: Web e Imagem verdes só quando confirmam**; visor com zoom; faixa copia no clique.
5. **Checagem — regra 4:** ligação com qualificação SIM com análise humana que sairia aprovada vai para revisão humana; a reprovada continua reprovada.
6. **Não é comércio:** clube de mães, serviço social, centro comunitário, beneficente, filantrópica, sem fins lucrativos, ONG (ligação 314534).
7. **Fotos:** capturadas as dos POIs mais próximos (até 60 m) das ligações sem imagem — 964 de 965.
8. **Julgar sem foto** (`--julgar-sem-foto`) as 2.221 cujo registro mais perto fica a mais de 60 m do hidrômetro.
9. **A 2563698** (162 registros, não cabe numa chamada da IA) foi direto para revisão humana — `scripts/producao_canoas/revisao_direta.py`.
10. **Publicar a ferramenta inteira em `/seek`**, com túnel próprio.

## Processos que continuam rodando no i9

| Laço | Script (cópia neste repositório) | Para como |
|---|---|---|
| Julgamento contínuo, a cada 10 min | `scripts/producao_canoas/julgamento_continuo.sh` (120 simultâneos, `--vinculo-novo --julgar-sem-foto`) | `touch ~/producao_canoas/julgamento_continuo/PARAR` |
| Reserva do Google | `scripts/producao_canoas/reserva_camoufox.sh` (até 10 rodadas, 1 h de pausa) | matar o `bash reserva_camoufox.sh` e `docker stop radar-busca-reserva` |
| Status | `scripts/producao_canoas/status_julgamento.sh` | — |

A reprovada que ganha resultado do Google no endereço volta sozinha ao julgamento no lote seguinte.

**Desde 14/09/2026, 15h50, os scripts de produção montam os arquivos da cópia de produção** (`~/producao/radarComercial`, tag `producao-2026-09-14.1`). O diretório de desenvolvimento pode ser editado sem mudar o que está no ar — ver `docs/DESENVOLVIMENTO.md`.

## Incidentes e correções da noite

| Quando | O quê | Correção |
|---|---|---|
| 13/09 22h55 | laço do julgamento parou ao ver "FIM" no progresso da busca | para só pelo arquivo `PARAR` |
| 14/09 04h28 | 2.633 processos zumbis do Camoufox; i9 com 70% de espera de disco | `docker run --init` |
| 14/09 07h53 | checagem reprovou as 6.883 aprovadas: comparava o nome exato do processo | aceita `enxuto de ...`; refeita do veredito da IA |
| 14/09 | respostas cortadas e 400 nos prédios grandes | teto de resposta pelo contexto medido (1,6 caractere/token no `/tokenize`); tempo limite pelo tamanho da resposta |

## Pendências

- Content-Security-Policy e monitor da VPS para `/seek` (`docs/PRODUCAO-SEEK.md`).
- 3 decisões oficiais na SEEK até agora; o trabalho humano de revisão começa.
- ~~Ambiente de desenvolvimento separado da produção~~ — feito em 14/09/2026: API dev na 7741 (Tailscale :8446), mesmo banco, `docs/DESENVOLVIMENTO.md`.
