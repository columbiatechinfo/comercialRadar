# Onde retomar — 02/09/2026

Este arquivo existe para uma sessão nova começar sabendo de tudo, sem precisar
reconstruir nada. Tudo aqui foi **medido**; onde houver estimativa, está dito.

---

## 1. Onde as coisas rodam, e como chegar nelas

**Nada de mineração roda no notebook.** Ele é terminal: manda comando por SSH e
lê log. A pasta local tem `.git`, documentação e memória de agente — o código
foi removido de propósito em 31/08.

| máquina | endereço | usuário | o que é |
|---|---|---|---|
| **i9 principal** | `100.66.173.63` (alias `i9`) | `a2l` | Ubuntu nativo, i9-14900K, 32 núcleos, 123 GB. É onde tudo roda. |
| **Predator** | `100.74.126.57` | `a2l` | Notebook Linux, 32 núcleos. Alcançável do notebook; **o i9 ainda não tem chave nele**. |
| **Spark** | `192.168.3.20` | — | vLLM, modelo `ia-principal` em `http://192.168.3.20:7400/v1` |
| **damasceno** | `100.87.227.69` | — | este notebook (Windows) |

```bash
ssh i9                        # do notebook, chave já configurada
ssh a2l@100.74.126.57         # Predator, do notebook
```

> **Cuidado:** existe um i9 ANTIGO em `100.115.117.49` (Windows + WSL, usuário
> `orbisgrid`). Não é este. Conferir com `grep -qi microsoft /proc/version`
> antes de qualquer conselho que dependa do sistema.

### Serviços de apoio, todos no i9

```
Postgres      container `supabase-db`, banco `a2l`
              psql como `supabase_admin` (o `migrator` não autentica por peer)
Nominatim     http://127.0.0.1:7200          geocodificação e reverso
OSRM carro    http://127.0.0.1:7300          rota; tem /tile/v1 até z18 (MVT)
OSRM a pé     http://127.0.0.1:7310
Proxies       Webshare, 500 IPs, `proxy_pool.ProxyPool` no repositório
Maps          MAPS_JS_KEY no .env do servidor
Hugging Face  o token está no .env sob o nome `huggingface`; a skill lê `HF_TOKEN`
```

### O repositório

O i9 é **cópia de trabalho com deploy key própria** — commita e empurra sozinho.
Não copiar arquivo de lá para o notebook para versionar.

```bash
# no i9
cd ~/Documentos/sistemas/radarComercial && git commit … && git push origin master
# no notebook
git fetch            # NUNCA pull nem checkout: traria os 276 .py de volta ao disco
```

---

## 2. O que mudou em 01/09, e está no GitHub

```
c88af84  perf(etapa1): a fusao, o parse de endereco e a traducao saem do passo 1
640a004  feat(etapa4): duas maquinas na mesma quadra, pela fila do banco
5227fa7  fix(etapa4): a memoria da sessao vira cookie, e o perfil em disco sai
6c0eaa9  fix(cadastur): a escrituracao do sistema sai da tabela base
f27e5c1  feat(etapa4): minerar pelo placeId, sem OCR e sem API paga
```

### Etapa 1 ficou crua

Saíram a fusão, o parser de endereço e a tradução de categoria. Não é flag: saiu
do código.

| | antes | agora |
|---|---|---|
| tempo (RS) | 32m33 | **2m49** |
| dedup | 25m05 · 41,7 mi de pares · 1,5 GB | — |
| entregues | 972.728 | 1.065.056 |
| colunas | 44 | 33 |
| validação | REPROVADO | **APROVADO 12/12** |

O passo 1 guarda coordenada, CEP e o endereço como a fonte escreveu.
`cluster_id` vale `fonte:id_fonte` e a etapa 2 não mudou.

**Consequência aberta:** sem fusão no passo 1, a etapa 2 importa duplicatas
entre fontes. Medido na saída do RS: 31.056 estabelecimentos aparecem em 2+
fontes, somando 62.318 linhas — cerca de **31 mil linhas a colapsar**. Dentro da
mesma fonte a repetição é resíduo: 957 linhas em 1.065.056 (0,09%).

### Etapa 4 mina pelo placeId, sem OCR e sem API paga

O `placeId` sai no próprio evento de clique do mapa JS; `e.stop()` impede o
cartão de abrir, e é o cartão que dispara o `GetPlace` cobrado. Medido: 64
chamadas cobradas por tile → **0**.

Com o id, abre-se `google.com/maps/place/?q=place_id:<ID>` por proxy. Na quadra
de Canoas, 90 POIs: **90/90 com nome e endereço**, 79 com telefone, 861
avaliações, 736 fotos, **zero chamada cobrada**.

A memória da sessão é um **cookie** (`storage_state`, 999 bytes), não perfil em
disco — perfil dá HTTP 407 na segunda execução e pesa 52 MB. Sem cookie o Google
serve ficha reduzida, sem a aba de avaliações, e o zero resultante é
indistinguível de "não tem avaliação". Ver `docs/adr/0006`.

### Cadastur: a escrituração saiu da base

Nasceu `radar_comercial.cadastur_vinculo` (migração 0015), com FK para `pois`
`ON DELETE CASCADE`. Antes, `poi_id` e `sem_poi_motivo` moravam em
`resources_root.cadastur_prestador` e sobreviviam à limpeza, deixando ponteiro
morto — a etapa 3 dizia "pendentes 0" numa cidade com 185 prestadores.

---

## 3. iFood — o que foi apurado em 01/09

O objetivo: obter os **ids** das lojas de uma área. O detalhe vem depois, pela
rota de API, que é gratuita.

### As barreiras, medidas

| caminho | resultado | tempo |
|---|---|---|
| `requests` puro, direto e por proxy | **403** | — |
| Scrapling `Fetcher` (TLS de Chrome/Firefox/Safari) | **403** nos três | 0,0 s |
| Scrapling `DynamicFetcher` (Playwright) | **403** | 31 s |
| Scrapling `StealthyFetcher` (Camoufox) | **passa**, resolve captcha Cloudflare | 68 s a frio |

Proxy não muda nada: **não é reputação de IP**. É PerimeterX (`PXO1GDTa7Q_px_*`
no localStorage) mais Cloudflare. Imitar TLS não basta; Playwright comum também
não. **Só o Camoufox atravessa.**

### O que funciona

```
sessão quente (StealthySession)   13–38 s → 2–3 s por página
o id está no HREF do cartão       /delivery/{cidade}/{slug}/{UUID}
                                  seletor: a.merchant-v2__link
/inicio traz TODAS as categorias  não precisa ir em /restaurantes, /mercados…
a lista vem COMPLETA              490 de uma vez; num navegador de gente
                                  aparecem ~20 e o botão "Ver mais"
                                  (.cardstack-nextcontent__button).
                                  O `network_idle` espera tudo carregar.
8 navegadores em paralelo         656 ids distintos, 2,2 min, 31% de sobreposição
```

O endereço entra em **dois botões**: "Confirmar localização" (mapa) e depois
"Salvar endereço" (formulário). Parar no primeiro não aplica.

O raio de entrega cruza municípios: dos 656, 562 em Canoas, 31 Esteio, 26
Cachoeirinha, 15 Sapucaia, 14 Porto Alegre.

### A rota de detalhe

```
GET https://marketplace.ifood.com.br/v1/merchants/{id}/extra
```

Sem token, sem navegador. (O `merchant-info/graphql` dá 403 do PerimeterX; o
`/v1/merchants/{id}` sem sufixo dá 400.) **Não reverificada em 01/09** — o
número de 60/60 com CNPJ 100% é da medição de 25/08 registrada no código.

### O único ponto aberto

**Como posicionar cada navegador sem digitar endereço.** Três tentativas
falharam, e as três por erro meu:

1. Nome de bairro no autocomplete — o iFood quer rua e número. Falhou em 4 de 8.
2. Coordenada revertida pelo Nominatim — **inventei o número da casa** ("Rua
   PQ12, 100") quando o reverso não trazia. Falhou em 7 de 8.
3. `set_geolocation` direto — a coordenada entra, mas o iFood **não oferece
   botão de "usar minha localização"** na landing. Falhou em 8 de 8.

Caminhos ainda não testados, do mais curto ao mais longo:

- **usar o endereço do próprio medoide** — o POI real tem `endereco` no banco,
  com rua e número de verdade. Era o que eu devia ter feito em vez de inventar;
- abrir o campo de endereço e procurar a opção de localização **lá dentro** (só
  olhei a landing);
- descobrir onde o iFood grava a posição após o fluxo manual e escrever direto.

### O medoide, e por que ele

A cidade é dividida numa grade 4×2 e cada célula dá o **medoide**: o POI real
mais próximo da mediana da célula. Mediana de latitude e de longitude tiradas
separadas dão um ponto que pode não ser lugar nenhum; o medoide sempre existe.
A consulta está em `docs/` como `medoides.sql` no histórico desta sessão; os 8
pontos de Canoas medidos foram:

```
celula  pois   lat          lng          medoide
2,0     9221   -29.928655   -51.172022   Porto de Pireu
2,1     5182   -29.903353   -51.169091   Casa Maira Massagista
1,0     4957   -29.922035   -51.184630   Mapi Soccer's
1,1     4185   -29.908444   -51.188198   Escolainfantil_semear
3,1     1984   -29.904627   -51.139649   Escolinha Encanto da Crianca
0,1     1284   -29.904610   -51.222734   Rua Santo Ângelo
3,0      853   -29.921345   -51.135889   (sem nome)
0,0       28   -29.924148   -51.217139   Bianchini S.A.
```

### Como rodar uma sonda de iFood

O Scrapling vive num container de pé chamado `scrapling` (roda como root porque
`scrapling install` chama `playwright install-deps`, que usa apt).

```bash
/tmp/rodar_scrapling.sh /tmp/minha_sonda.py
```

O script sobe o container e instala na primeira vez. As sondas de 01/09 estão em
`/tmp/ifood*.py` no i9.

---

## 4. Armadilhas que custaram tempo hoje

**PowerShell come barra invertida e barra vertical.** `ssh i9 'sed -i "s/\r$//"'`
chega no servidor como `s/r$//` e **apaga um `r` do fim de toda linha** — foi
assim que `return placar` virou `return placa`, e o erro só apareceu 12 minutos
depois. `grep -E "A|B"` vira pipeline do PowerShell. **Sempre escrever script em
arquivo e mandar por `scp`.** Para normalizar CRLF, fazer no Windows:

```powershell
$txt = [IO.File]::ReadAllText($origem) -replace "`r`n", "`n"
```

**`docker run`/`docker exec` sem `-i` descarta heredoc em silêncio** — o comando
sai com código 0 e não fez nada.

**Cache de etapa não invalida com mudança de código.** O `normalize` da skill é
retomável por lote e pula o lote cujo arquivo existe; o `raw` idem. Mudei o
código, o `normalize` levou 1 s e entregou o resultado de 31/08 — quase reportei
uma medição que não media o que eu disse. Apagar os artefatos antes de medir.

**`innerText` depende de layout** e volta vazio para elemento fora de vista. Foi
o que escondeu as avaliações do Maps e o botão do iFood. Usar `textContent`, ou
classe, ou `aria-label`.

**A imagem `radar-minerador` roda como usuário sem privilégio**, e os artefatos
de execuções anteriores são do root. Para apagá-los, container com `--user root`.

**`chrome-headless-shell` estoura com SIGSEGV** e se anuncia (`HeadlessChrome`
nas marcas). Usar Chromium completo sob Xvfb, com
`--disable-blink-features=AutomationControlled`. O `xvfb-run` trava neste
container: subir o `Xvfb` à mão e passar `DISPLAY`.

---

## 5. A fila do que vem

1. **iFood** — resolver o posicionamento (caminho 1 acima), depois ligar a
   colheita de ids à rota `/extra` e gravar como fonte.
2. **Airbnb** — mesma técnica (Scrapling), listar os locais de uma cidade/área e
   entrar item por item colhendo nome do dono, avaliações, fotos, localização.
   Vira fonte nova.
3. **Etapa da área** — é ela que fecha o buraco aberto pela etapa 1: fusão +
   normalização de endereço (skill e IA) + identificação de categoria contra a
   taxonomia do Google, sobre os POIs do município. As funções pesadas já existem
   em `skills/extracao-poi-estadual/poi_estadual/vendor/` (`parse_endereco`,
   `traduzir`, `dedup_pois_auditado`).
4. **Comando separado de normalização** por skill sobre a base do cliente, a cada
   atualização da base, sem interferir no processo.
5. **Chave SSH** do i9 no Predator, para os navegadores de transbordo.
6. **Pool residente** de navegadores (20 no i9, 15 no Predator), nunca fechados,
   renovados uma vez por dia por comando.

### Decisões já tomadas, para não relitigar

- etapa 5 (descoberta por categoria) **removida** — o placeId tornou-a inútil
- OCR **removido**; os tiles continuam, para telhado
- zoom **20** — z19 pega 53%, z18 pega 22%, e a perda não volta com mais passadas
- varredura **adaptativa**, passo de meio tile
- tiles: só o do Google, em WebP no Storage do Supabase; o OSRM não tem telhado
- curtida em POI **fora** — exigiria conta logada
