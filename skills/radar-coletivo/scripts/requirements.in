# Radar Coletivo — FAIXAS aceitas (contrato amplo). A validação real está em
# requirements.lock.txt (versões exatas testadas pela suíte). Instalar do
# .lock em produção; o .in serve para regenerar o lock após revalidação.
pandas>=2.1,<4
numpy>=1.26,<3
openpyxl>=3.1,<4
scikit-learn>=1.3,<2
